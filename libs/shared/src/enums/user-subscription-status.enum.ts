export enum UserSubscriptionStatus {
  INCOMPLETE = 'incomplete',
  INCOMPLETE_EXPIRED = 'incomplete_expired',
  ACTIVE = 'active',
  PAST_DUE = 'past_due',
  CANCELING = 'canceling',
  CANCELED = 'canceled',
  UNPAID = 'unpaid',
}
